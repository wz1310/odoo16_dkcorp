<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_mrp_form_approval_inherit" model="ir.ui.view">
        <field name="name">mrp.form.approval.inherit</field>
        <field name="model">mrp.production</field>
        <field name="inherit_id" ref="mrp.mrp_production_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="approving_matrix" string="Approve Matrix" type="object" 
                        class="oe_highlight" 
                        attrs="{'invisible': ['|',('user_can_approve', '=', False),('state','!=','confirmed')]}"
                        />
                <button name="rejecting_matrix" string="Reject Matrix" type="object" 
                        class="btn-danger" 
                        attrs="{'invisible': ['|',('user_can_approve', '=', False),('state','!=','confirmed')]}"/>
            </xpath>

            <xpath expr="//notebook" position="inside">
                <page string="Approval Matrix" name="approval_matrix">
                    <group>
                        <group>
                            <field name="approved" invisible="1" readonly="1"/>
                            <button string="Fetch Matrix" type="object" name="checking_approval_matrix" class="oe_highlight" groups="wz_akses_group.group_fetch_matrix" />
                            <field name="user_can_approve" invisible="1"/>
                        </group>
                    </group>
                    <field name="approval_ids" readonly="1">
                        <tree>
                            <field name="approver_seq"/>
                            <field name="approver_ids" widget="many2many_tags"/>
                            <field name="approved_by_ids" widget="many2many_tags"/>
                            <field name="rejected_by_ids" widget="many2many_tags"/>
                            <field name="minimum_approved"/>
                            <field name="approved"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>